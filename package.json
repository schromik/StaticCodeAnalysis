{
  "name": "StaticCodeAnalysis",
  "version": "1.0.0",
  "description": "Statische Codeanalyse mit ESLint und Semgrep",
  "scripts": {
    "lint": "eslint ./thisCodeGetsTested",
    "semgrep": "semgrep ./thisCodeGetsTested --config=auto && semgrep ./thisCodeGetsTested -f semgrepRules.yml"
  },
  "author": "Sebastian Chromik",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.32.0",
    "eslint-plugin-custom-rules": "file:eslint"
  }
}
